{
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "outputDirectory": "subframe-cursor-app/dist",
  "version": 2,
  "buildCommand": "cd subframe-cursor-app && npm install && npm run build",
  "installCommand": "cd subframe-cursor-app && npm install",
  "framework": "vite"
} 