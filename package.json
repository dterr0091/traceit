{
  "name": "traceit",
  "version": "1.0.0",
  "description": "Content tracing and analysis platform",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd subframe-cursor-app && npm run build",
    "dev": "cd subframe-cursor-app && npm run dev",
    "test": "cd subframe-cursor-app && npm run test",
    "lint": "cd subframe-cursor-app && npm run lint",
    "preview": "cd subframe-cursor-app && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
